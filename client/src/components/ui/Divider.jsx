import React from 'react';

/**
 * Warm hairline horizontal divider.
 * Uses the `line` color token: rgba(140,106,100,0.2).
 */
const Divider = () => (
    <div className="h-px bg-line" />
);

export default Divider;
